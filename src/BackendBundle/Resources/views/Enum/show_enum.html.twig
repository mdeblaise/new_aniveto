{% extends "SonataAdminBundle:CRUD:base_show_field.html.twig" %}
{% block field %}
    {% spaceless %}
            {{ value|enum_label(field_description.options.class, field_description.options.catalogue|default('messages'), field_description.options.class_prefix|default(true)) }}
    {% endspaceless %}
{% endblock %}